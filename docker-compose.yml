version: '3'

services:
  jupyterlab:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - home:/home/wonky
    network_mode: host

volumes:
  home:
